version: "3.9"

services:
  vpn-bot-server:
    container_name: vpn-bot-server
    build:
      context: https://github.com/WoodieDudy/wg-vpn-tg-bot.git#main
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    privileged: true
    sysctls:
      - net.ipv4.ip_forward=1
      - net.ipv4.conf.all.src_valid_mark=1
    environment:
      # --- Bot environment ---
      BOT_TOKEN: ${BOT_TOKEN}
      ADMIN_ID: ${ADMIN_ID}

      # --- WireGuard server settings inside container #1 ---
      WG_SERVER_INTERFACE: ${WG_SERVER_INTERFACE:-wg0}
      WG_SERVER_PORT: ${WG_SERVER_PORT:-51820}
      WG_SERVER_SUBNET: ${WG_SERVER_SUBNET:-10.66.0.0/24}
      WG_SERVER_ADDRESS: ${WG_SERVER_ADDRESS:-10.66.0.1/24}
      WG_SERVER_PRIVATE_KEY: ${WG_SERVER_PRIVATE_KEY}

      # Traffic from WG users in container #1 will be routed to container #2
      WG_CLIENT_GATEWAY_IP: ${WG_CLIENT_GATEWAY_IP:-172.30.0.2}
      WG_CLIENT_EGRESS_IFACE: ${WG_CLIENT_EGRESS_IFACE:-eth0}
    ports:
      - "${WG_SERVER_PORT:-51820}:${WG_SERVER_PORT:-51820}/udp"
    volumes:
      - ./config/wg-server:/etc/wireguard
      - ./scripts/container1-entrypoint.sh:/usr/local/bin/container1-entrypoint.sh:ro
    entrypoint: ["/bin/sh", "/usr/local/bin/container1-entrypoint.sh"]
    networks:
      transit:
        ipv4_address: 172.30.0.10
    depends_on:
      - wg-egress-client
    restart: unless-stopped

  wg-egress-client:
    image: alpine:3.20
    container_name: wg-egress-client
    cap_add:
      - NET_ADMIN
    sysctls:
      - net.ipv4.ip_forward=1
    volumes:
      # Укажите путь к клиентскому конфигу внешнего WG-сервера через WG_CLIENT_CONFIG_PATH.
      # По умолчанию: ./config/wg-client/wg-egress.conf -> /etc/wireguard/${WG_CLIENT_INTERFACE}.conf
      - ${WG_CLIENT_CONFIG_PATH:-./config/wg-client/wg-egress.conf}:/etc/wireguard/${WG_CLIENT_INTERFACE:-wg-egress}.conf:ro
      - ./scripts/container2-entrypoint.sh:/usr/local/bin/container2-entrypoint.sh:ro
    entrypoint: ["/bin/sh", "/usr/local/bin/container2-entrypoint.sh"]
    networks:
      transit:
        ipv4_address: 172.30.0.2
    restart: unless-stopped

networks:
  transit:
    driver: bridge
    ipam:
      config:
        - subnet: 172.30.0.0/24
